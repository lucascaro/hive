# Theme presets groundwork

Theme presets groundwork: `localStorage['hive.theme']` accepts `classic` (default), `hive-dark`, `hive-light`, `system`. No visual change by default, except that the terminal cursor and selection now use the accent colour.
